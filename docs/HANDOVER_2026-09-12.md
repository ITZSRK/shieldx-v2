# Handover — shieldx-v2 website session, written 2026-09-12

Written before a Claude Code restart ends this session's context, at the
request of the HQ coordination session. The work below was done **2026-09-01
and 2026-09-02**; the session then sat idle ten days. Everything here was
re-verified on 2026-09-12 before being written down.

---

## 1. The lane, and the deploy risk

This is the **public website repo** — `queloshieldx.in`, S3 + CloudFront +
Route 53.

**Confirmed, and worse than assumed: `main` has no review gate at all.**

- `.github/workflows/deploy.yml` triggers on `push: branches: [main]`
- It runs `aws s3 sync dist/ s3://shieldx-v2-site/ --delete` then a CloudFront
  invalidation of `/*`
- `gh api repos/ITZSRK/shieldx-v2/branches/main/protection` returns
  **404 "Branch not protected"**

So any push to `main` — including a force-push, including one that deletes
files, since the sync uses `--delete` — reaches the live public site with no
PR, no approval and no CI gate. The staging design document's flag is correct
and understated.

The compensating control is a human convention recorded in `CLAUDE.md`: build
locally, screenshot, get SRK's approval before pushing. It is convention only.
Nothing enforces it.

---

## 2. Where the work stands

Seven stacked PRs, **all open, none merged, nothing deployed**. Verified
2026-09-12: the live site still serves the old copy
(`<title>Real-Time Decisioning Infrastructure — ShieldX</title>`).

| PR | Branch | Contents |
|---|---|---|
| #2 | `pr-a-nav-touch-diya` | "Diya" removed site-wide (9 places, 6 files); capability cards fixed for touch; Integration added to nav **then removed again** (see §4) |
| #3 | `pr-b-homepage-copy` | Homepage reorder; category → Customer Decisioning Infrastructure; neutrality line; founder line; levers lede; Agency-lever trim |
| #4 | `pr-c-hero-lcp` | Hero image `width`/`height` + `alt` |
| #5 | `pr-d-company` | Founder block reworked; legal entity removed from `/company` body (footer-only); `FOUNDING TEAM` → `FOUNDER` |
| #6 | `pr-e-orchestrate` | `Orchestrate` codename → `Sequencing` |
| #7 | `pr-f-wedge` | Hero wedge line — collections as entry, lifecycle as scope |
| #8 | `pr-g-hero-density` | `CLAUDE.md` governing sentence corrected; hero vertical rhythm tightened; two cosmetic nits |

Merge order is **#2 → #3 → #4 → #5 → #6 → #7 → #8**. Each is based on the one
before it, so merging out of order will produce confusing diffs.

**Next step, if resuming:** the only unblocked item is a **two-number
capability strip** for `/platform` (brief item #5) — 4 compliance checks and
90-day retention, both evidenced (see §5). It was offered and not answered
before the session went idle.

### Changed under me while idle

- **`origin/main` moved.** PR #9 (gitleaks secret-scan CI) merged 2026-09-08.
  It adds only `.github/workflows/secret-scan.yml`, which no branch in the
  stack touches, and GitHub still reports #3–#8 `MERGEABLE / CLEAN`. The stack
  is one commit behind main but not in conflict.
- **This handover branch is cut from the new `origin/main`**, not from the
  stack, so it carries none of the above.

---

## 3. State outside this repo

Things a future session would otherwise find and not be able to explain.

**`~/shieldx-hq/decisions_inbox.md` — 7 blocks appended 2026-09-02.** Dated
`2026-09-02`, tagged DECISION / CONFLICT / FINDING / OPEN, each ending
`**Source:** chat (Claude Code, shieldx-v2 session)`. They were left
uncommitted deliberately — HQ had other work in flight and bundling into it
was not mine to do. **They have since been committed by someone else and are
still present.** The inbox describes itself as a transit lane, so they are
meant to be filed into 03/04/07/cleanup_log and deleted, not left sitting.

**A local preview server** was running on `127.0.0.1:4173` (`vite preview`
against `dist/`). It was killed 2026-09-02. Nothing depends on it.

**Screenshots and a backup** were written to the session scratchpad under
`/private/tmp/claude-501/...`. That path is ephemeral and should be assumed
gone. Nothing references it.

**No AWS, DNS, S3 or CloudFront state was touched.** No deploy was run. No
credentials were used or created.

---

## 4. Things tried that did not work, and why

Kept because each one cost real time to discover and the wrong fix looks
reasonable on paper.

**Nav breakpoint — two failed attempts before the right one.** Adding
Integration made six nav items, which overflow at 768px. First fix moved the
breakpoint `md` → `lg`; that produced a near-empty bar with a lone hamburger
across 900–1023px, visibly worse than the problem. Reverting to `md` after
dropping Integration was *also* wrong: measured, the five-item row **wraps
below 840px** — at 768 "How we deploy" breaks across three lines, the logo
collides with "Platform", the CTA overlaps "Company". That was true on `main`
too; `md` had never actually fit. Landed on a measured `min-[860px]`.
**Neither stock Tailwind breakpoint is correct for this nav.**

**Hero top padding.** `pt-20` (80px) exactly equals the fixed nav height, so
the eyebrow pill sat flush on the nav's bottom border with zero clearance.
`pt-28` gives 32px desktop / 44px mobile.

**Hero image: the textbook LCP fix made it worse.** The hero PNG is the LCP
element and was `loading="lazy"` — normally a bug. Setting it eager with
`fetchPriority="high"` pushed **LCP 4.9s → 6.2s and TBT 30ms → 100ms**,
because 239KB of PNG competes with the JS bundle on a throttled link. Reverted
to lazy; kept `width`/`height` (fixes CLS → 0) and `alt` (a11y 90→96, SEO
92→100). **The real fix is a WebP encode, ~210KB saved per Lighthouse** — not
attempted, because it is the only product imagery on the site and re-encoding
it is SRK's call.

**A Lighthouse run that reported a false 100.** Passing `--preset=desktop`
alongside mobile screen-emulation flags let the preset's *throttling* win while
`formFactor` stayed mobile. Reported Performance 100. Real mobile numbers are
**79–80**. Any future run must not mix a preset with manual emulation flags.

**Two rejected drafts of the homepage neutrality line.** "ShieldX never
operates collections" scoped the company to collections one screen after the
hero claims the credit lifecycle. The obvious repair — "never operates the
channels it scores" — is **factually false**: ShieldX runs a voice channel, and
Neutrality Charter commitment 2 says its own channels are scored by the
identical pipeline. SRK caught it. "Umpire, not player" was dropped for the
same reason — ShieldX *is* a player in the automated channels. Shipped wording
joins commitment 1 to commitment 2 and names the voice channel explicitly.

**A rebase that pushed mid-conflict.** Amending a commit in `pr-a` while `pr-b`
and `pr-c` were stacked on its old version caused a conflict, and `&&`
chaining pushed the branches while the rebase was unresolved. Recovered with
`git rebase --skip` on the superseded commit. The stack was re-verified linear
and in sync afterwards. **Do not chain `git push` after `git rebase` with
`&&`.**

---

## 5. Belief vs. verified fact

**Verified:**

- `main` auto-deploys with no branch protection (§1) — checked directly.
- The seven PRs are open and unmerged; the live site serves the old copy —
  checked 2026-09-12.
- No decision-latency p95 exists anywhere in `~/shieldx-hq`. Every latency
  figure there is third-party provider latency (Digitap 37.2s, Deepvue 0.6s),
  LLM time-to-first-token ~0.9s, or an Assist *target* of ≤2000ms. The
  **"~1 second" asserted on `/platform` is unevidenced.**
- The brief's "7 compliance checks" has no support. `ComplianceGate.jsx`
  defines **four**.
- Retention: **90 days** for Tier 1 borrower content per
  `data_retention_position.md`. This is a *different* figure from the privacy
  policy's 3-year contact-data number; both are true of different data.
- Of the three commits parked on local `main`, only `ed653d8` reaches the
  built site — `b62207e` is `CLAUDE.md` and `cb690e2` is `infra/`, neither of
  which the Vite build includes.

**Believed, not verified — treat with suspicion:**

- *I believe* the three parked commits on local `main` were parked
  deliberately. SRK said "leave the old commits" but never said why they were
  parked. They ride along inside every PR in the stack, so **merging any PR
  ships `ed653d8` to the live site.**
- *I believe* the Lighthouse figures are directionally right for production,
  but they were measured against a **local `vite preview`**, not production
  CloudFront. Absolute numbers will differ.
- *I have not verified* that a CI build produces byte-identical output to my
  local `npm run build`.
- *I did not check the platform-backend repo* for a p95. The claim in §5 is
  "not in HQ", not "does not exist anywhere".

---

## 6. The naming conflict is now worse, not resolved

Flagged 2026-09-02 and **still open**. Three names are now in play for the same
thing:

| Source | Name |
|---|---|
| Website change brief (1 Sept), as a "do not rename" guardrail | **ShieldX RightParty** / **RightParty Review** |
| `naming_decisions_RATIFIED.md` (ratified 25 Aug, self-declared source of truth) | **Contact Readiness Review** (engagement) / **Right-Party Reachability** (module) |
| New untracked HQ build docs (`right_party_contact_module.md`, `rpc_v0_build_plan.md`, post-2 Sept) | **Right-Party Contact (RPC)** |

My CONFLICT block is at `decisions_inbox.md:1093`, unanswered.

**Brief item #1 (`/rightparty-review`) must not be built until this resolves.**
It would be the first public use of whichever name is chosen. Two further
blockers behind it: no scope ceiling is documented anywhere, and
`infra/demo-form/index.mjs` has a hardcoded `FIELDS` allowlist that silently
drops unknown fields, so `engagement=review` needs a Lambda deploy.

---

## 7. If nothing else is read

1. `main` deploys to the public internet with no gate. Treat every push to it
   as a production release.
2. Seven PRs are built, reviewed and waiting. Nothing is live.
3. Merging any of them also ships a parked commit (`ed653d8`) that was not part
   of this work.
4. Do not build the RightParty page. Three names are in play.
